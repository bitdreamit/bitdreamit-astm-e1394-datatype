<component name="ArtifactManager">
  <artifact type="jar" build-on-make="false" name="bitdreamit-astm-e1394-datatype-server">
    <output-path>$PROJECT_DIR$/out/artifacts/bitdreamit_astm_e1381_transmission_server</output-path>
    <root id="archive" name="bitdreamit-astm-e1394-datatype-server.jar">
      <!--
        Server JAR contents (matches distribution/build.sh rule #6):
          - compiled classes from the `shared` module (Constants,
            Properties, Frame, FrameException, RetryMetrics)
          - compiled classes from the `server` module
            (ASTME1381StreamHandler, ASTME1381TransmissionModePlugin)

        NOTE (v1.1.7): plugin.xml and transmissionmode.xml are NO
        LONGER bundled inside the JARs. They are deployed as separate
        files at the extension folder root, per the Mirth Connect 4.x
        convention. Mirth's extension loader reads them from the
        filesystem, not from inside the JARs.

        To build via IntelliJ IDEA:
          Build -> Build Artifacts... -> bitdreamit-astm-e1394-datatype-server -> Build

        Output path:
          $PROJECT_DIR$/out/artifacts/bitdreamit_astm_e1381_transmission_server/
            bitdreamit-astm-e1394-datatype-server.jar

        For production deployment, also copy:
          $PROJECT_DIR$/plugin.xml
          $PROJECT_DIR$/transmissionmode.xml
        to the Mirth extension folder alongside this JAR.
      -->
      <element id="module-output" name="shared" />
      <element id="module-output" name="server" />
    </root>
  </artifact>
</component>
